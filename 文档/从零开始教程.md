# 从零开始：小米 AX3000T 刷 OpenWrt + 校园网自动认证 完整教程

> 适用人群：零基础小白。你不需要懂 Linux、不需要懂编程。
> 只要你有一台 Windows 电脑、一根网线、一台 AX3000T 路由器，跟着本教程，你就能完成从刷机到自动上网的全部操作。
>
> **预计总耗时**：2-4 小时（新手） / 1-2 小时（有经验）
>
> **难度**：刷机部分 ⭐⭐⭐ / 配置部分 ⭐⭐
>
> 本教程基于真实项目记录——作者是河南大学学生，路由器型号 AX3000T (v1)，目标校园网为 henu-student。

---

## 目录

- [Part 0：准备——你需要的硬件和软件](#part0)
- [Part 1：解锁 SSH——获得路由器控制权](#part1)
- [Part 2：刷入 ImmortalWrt——装上新系统](#part2)
- [Part 3：系统初识——认识你的新路由器](#part3)
- [Part 4：安全加固——设密码、配 WiFi](#part4)
- [Part 5：WiFi WAN——让路由器无线连校园网](#part5)
- [Part 6：认证脚本——自动登录校园网](#part6)
- [Part 7：定时重连——永远不掉线](#part7)
- [Part 8：日常维护——出问题了怎么办](#part8)
- [附录 A：背后的原理（写给想深入理解的人）](#appendix-a)
- [附录 B：常见问题 FAQ](#appendix-b)
- [附录 C：拓展玩法](#appendix-c)

---

<a name="part0"></a>
## Part 0：准备

### 硬件清单

| 物品 | 用途 | 备注 |
|------|------|------|
| 小米 AX3000T 路由器 | 主角 | v1 版本（生产日期 2024 年及之前） |
| 路由器原装电源 | 供电 | |
| 一台 Windows 电脑 | 操作路由器 | 有网口最好，没有就买个 USB 转网口 |
| 一根网线 | 电脑连路由器 | 一般买路由器会送 |

### 软件清单

| 软件 | 用途 | 下载 |
|------|------|------|
| XMiR-Patcher | 解锁 SSH | 已包含在教程文件夹 `step1解锁ssh/xmir-patcher-main/` |
| ImmortalWrt 固件 | 新系统 | 已包含在 `step2刷入openwrt/` |
| SSH 客户端 | 远程连接路由器 | 教程用 Git Bash 或 Windows 自带 OpenSSH |

### 心理准备

1. **不要慌**。只要不把 Factory 分区擦掉，路由器几乎不可能变砖。
2. **跟着教程一步步来**，不要跳步骤。
3. **操作前先读完整段说明**，再动手。
4. 如果出问题，有`恢复原厂系统`文件夹可以救回来。

---

<a name="part1"></a>
## Part 1：解锁 SSH

### 什么是 SSH？为什么需要解锁？

小米原厂固件是锁死的——你不能用 SSH 远程登录路由器，只能通过米家 App 或 Web 后台管理。**刷机的前提是获得 SSH 访问权限**，相当于"获取路由器的 root 权限"。

我们使用的工具叫 **XMiR-Patcher**（开源项目，GitHub: openwrt-xiaomi/xmir-patcher），它利用小米固件的漏洞来解锁 SSH。

### 操作步骤

#### 步骤 1.1：连接电脑和路由器

```
  电脑                  AX3000T 路由器
  ┌──────┐             ┌──────────────┐
  │      │   网线接    │              │
  │ 网口 ├────────────→│ LAN4 口       │
  │      │             │              │
  └──────┘             └──────┬───────┘
                              │ 插电源
                              │
                           开机等 2 分钟
```

- 路由器先用网线连电脑，再通电开机。
- 等路由器完全启动（指示灯变白/蓝，大约 2 分钟）。
- 此时你的电脑应该能通过浏览器打开 `miwifi.com` 或 `192.168.31.1` 看到小米的设置页面。

> **如果之前已经用过这个路由器**（已经设置过了），确保路由器处于正常联网状态（可以不用连 WAN 口，但路由器系统必须已启动）。

#### 步骤 1.2：运行 XMiR-Patcher

进入教程文件夹：
```
小米AX3000T刷机教程openwrt大分区版/
└── step1解锁ssh/
    └── xmir-patcher-main/
        ├── !START.bat          ← 双击这个
        ├── python/             (内置 Python，不需要你自己装)
        └── menu.py             (菜单程序)
```

**操作方法**：
1. 双击 `!START.bat`
2. 会弹出一个命令行窗口，显示菜单
3. 根据菜单选择以下操作：

```
菜单大致结构（你的版本可能略有不同）：

[1] Connect to router        ← 第一步：连接路由器
[2] Read router info         ← 第二步：读取路由器信息  
[3] Create backup            ← 第三步：备份（非常重要！）
[4] Unlock SSH               ← 第四步：启用 SSH
[5] Install uboot            ← 第五步：刷入新 U-Boot
[6] Set env variables        ← 第六步：设置启动变量
```

> **⚠️ 关键提醒**：
> - **一定要先做 `Create backup`（备份）！** 这会把 Factory 分区（WiFi 校准数据）备份到 `backups/` 文件夹。如果丢了这些数据，WiFi 信号会废掉且无法恢复。
> - 备份文件会自动放在 `xmir-patcher-main/backups/` 目录下，**请把这些文件复制到安全的地方备份**（网盘、U盘等）。

#### 步骤 1.3：确认 SSH 已解锁

解锁成功后，你可以在命令行中：
```bash
ssh root@192.168.31.1
```

如果提示输入密码或直接进入命令行，说明 SSH 已经通了。

> **你可能需要先安装/启用 OpenSSH 客户端**：
> Windows 10/11：设置 → 应用 → 可选功能 → 添加功能 → OpenSSH 客户端

---

<a name="part2"></a>
## Part 2：刷入 ImmortalWrt

### 什么是 ImmortalWrt？

ImmortalWrt 是 OpenWrt 的一个中国社区维护版本。相比原生 OpenWrt，它：
- 预装中文语言包
- 预配国内软件镜像源（更新/装包更快）
- 对中国用户的网络环境做了优化

我们使用的是 **大分区版**——把原来小米切得很碎的分区合并成一个大分区，让你有 78MB 可用空间（原版只有 10MB 左右）。

### 操作步骤

#### 步骤 2.1：连接路由器 SSH

```bash
ssh root@192.168.31.1
```

#### 步骤 2.2：上传固件文件

固件文件：
```
step2刷入openwrt/
└── immortalwrt-24.10.5-mediatek-filogic-xiaomi_mi-router-ax3000t-squashfs-sysupgrade.bin
```

**方法 A：通过 SCP 上传**（推荐）
```bash
# 在 Windows 命令行/Git Bash 中执行
# 把固件传到路由器的 /tmp 目录
scp "step2刷入openwrt/immortalwrt-24.10.5-...sysupgrade.bin" root@192.168.31.1:/tmp/
```

**方法 B：通过 XMiR-Patcher 的 Install firmware 菜单**
- 工具菜单里有 "Install firmware" 选项
- 按照提示选择固件文件

#### 步骤 2.3：刷入固件

在路由器的 SSH 中执行：
```bash
# 查看上传的文件
ls -la /tmp/*.bin

# 刷入固件（用 sysupgrade 命令）
sysupgrade -n -v /tmp/immortalwrt-24.10.5-mediatek-filogic-xiaomi_mi-router-ax3000t-squashfs-sysupgrade.bin
```

参数说明：
- `-n`：不保留原配置（全新安装）
- `-v`：详细输出（能看到进度）

**刷机过程中**：
- 路由器会自动重启，指示灯会闪烁
- 整个过程大约 3-5 分钟
- **千万不要断电！** 断电可能导致变砖

#### 步骤 2.4：等待并验证

- 等路由器完全启动（指示灯稳定）。
- 打开浏览器访问 `http://192.168.1.1`（注意：刷完之后地址从 `192.168.31.1` 变成了 `192.168.1.1`）。
- 你应该看到 **LuCI**（OpenWrt 的 Web 管理界面），默认是中文界面。
- 用户名：`root`，密码为空（直接点"登录"）。

---

<a name="part3"></a>
## Part 3：系统初识

### 3.1 登录 Web 管理界面

```
浏览器打开：http://192.168.1.1
用户名：root
密码：（空，直接登录）
```

登录后你会看到 ImmortalWrt 的 LuCI 界面——相当于路由器的"桌面"。

### 3.2 认识 LuCI 界面

```
┌──────────────────────────────────────────┐
│  ImmortalWrt                   状态总览   │
├──────────────────────────────────────────┤
│  菜单栏                                   │
│  ├── 状态                                 │
│  │   ├── 概览        ← CPU/内存/网络状态   │
│  │   ├── 系统日志    ← 出问题时看这个      │
│  │   └── ...                             │
│  ├── 系统                                 │
│  │   ├── 系统        ← 主机名、时区        │
│  │   ├── 管理权      ← 改密码             │
│  │   ├── 软件包      ← 装/删软件           │
│  │   ├── 启动项      ← 开机自启           │
│  │   └── ...                             │
│  ├── 网络                                 │
│  │   ├── 接口        ← 网络接口配置        │
│  │   ├── 无线        ← WiFi 配置          │
│  │   ├── DHCP/DNS    ← 地址分配和域名解析   │
│  │   ├── 防火墙      ← 安全规则            │
│  │   └── ...                             │
│  └── 退出                                 │
└──────────────────────────────────────────┘
```

### 3.3 用 SSH 连接（推荐学会）

除了 Web 界面，更强大的管理方式是通过 SSH 命令行：

```bash
ssh root@192.168.1.1
```

> **为什么要学 SSH？**
> Web 界面能做的大多数配置，SSH 都能做，而且更灵活、可批量操作、可写脚本自动化。
> 本教程后面的很多操作会给出 SSH 命令，因为命令比截图更精确。

---

<a name="part4"></a>
## Part 4：安全加固

> **现状**：刷完的系统 WiFi 无密码、root 无密码——任何人连上你的路由器都能完全控制它。

### 4.1 设置 root 密码

**方法 A：通过 Web 界面**

系统 → 管理权 → 输入新密码 → 保存并应用

**方法 B：通过 SSH**

```bash
ssh root@192.168.1.1
passwd
# 输入新密码，回车
# 再次输入，回车
```

> 💡 **密码建议**：至少 8 位，包含字母、数字、特殊符号。如 `Czj20070312#`
> 
> 设置后，Web 登录和 SSH 登录都需要这个密码。

### 4.2 设置 WiFi 密码

**方法 A：通过 Web 界面**

网络 → 无线 → 找到 2.4G 和 5G 的接口 → 编辑 → 无线安全 → 选 WPA2-PSK → 输入密码 → 保存并应用

**方法 B：通过 SSH**

```bash
# 设置 2.4G WiFi
uci set wireless.default_radio0.ssid='你的WiFi名'
uci set wireless.default_radio0.encryption='psk2'
uci set wireless.default_radio0.key='你的WiFi密码'

# 设置 5G WiFi
uci set wireless.default_radio1.ssid='你的WiFi名'
uci set wireless.default_radio1.encryption='psk2'
uci set wireless.default_radio1.key='你的WiFi密码'

# 保存并立即生效
uci commit wireless
wifi reload
```

参数说明：
- `psk2` = WPA2-PSK（目前最常用的 WiFi 加密方式，兼容性好）
- `sae` = WPA3（新一代加密，更安全但老设备可能不支持）

### 4.3 配置 SSH 密钥免密登录（可选但推荐）

每次 SSH 都输密码既麻烦又不够安全。SSH 密钥用"非对称加密"实现免密登录：

```bash
# 在你的 Windows 电脑上（不是路由器上）：
# 1. 生成密钥对
ssh-keygen -t ed25519 -f ~/.ssh/ax3000t_rsa -N ""

# 2. 把公钥上传到路由器
scp ~/.ssh/ax3000t_rsa.pub root@192.168.1.1:/tmp/

# 3. SSH 到路由器，添加公钥
ssh root@192.168.1.1
mkdir -p /etc/dropbear
cat /tmp/ax3000t_rsa.pub >> /etc/dropbear/authorized_keys
chmod 600 /etc/dropbear/authorized_keys

# 4. 之后免密登录
ssh -i ~/.ssh/ax3000t_rsa root@192.168.1.1
```

> 💡 创建 SSH config 文件 `~/.ssh/config`：
> ```
> Host ax3000t
>     HostName 192.168.1.1
>     User root
>     IdentityFile ~/.ssh/ax3000t_rsa
> ```
> 之后只需输入 `ssh ax3000t` 就能连接。

---

<a name="part5"></a>
## Part 5：WiFi WAN——让路由器无线连校园网

这是整个项目**最核心的一步**。

### 前置知识：AP vs STA

```
AP 模式（Access Point）        STA 模式（Station）
   你连 WiFi 热点的模式           WiFi 设备连路由器的模式
   ┌──────────┐                 ┌──────────┐
   │路由器发射WiFi│               │路由器接收WiFi│
   │让别人连      │               │去连校园网    │
   └──────────┘                 └──────────┘
```

默认情况下，两个 radio 都是 AP 模式。我们需要把其中一个改成 STA 模式去连接校园网。

### 5.1 安装必要软件

虽然 `wpad-openssl`（包含 STA 客户端功能）通常预装了，但确认一下：

```bash
ssh root@192.168.1.1
opkg update
opkg list-installed | grep wpad
```

如果输出里有 `wpad-openssl`，就说明已经有了。

### 5.2 扫描校园网 WiFi

```bash
# 先看有什么 WiFi 信号
iw dev phy0-ap0 scan | grep -E "SSID|signal" | head -20
```

> **⚠️ 注意**：`iw scan` 在某些路由器上可能导致 WiFi 短暂断连。如果扫描后 SSH 断开，等 30 秒再重连即可。只扫一次，不要反复扫。

你会看到类似输出：
```
SSID: henu-student
signal: -56.00 dBm
SSID: henu-student
signal: -63.00 dBm
...
```

记下信号最好的那个。dBm 越接近 0 越好。

### 5.3 决定用哪个 radio 连接校园网

| radio | 频段 | 建议用途 | 理由 |
|-------|------|----------|------|
| radio0 | 2.4G | 宿舍 AP（发射热点） | 穿墙好，覆盖广 |
| radio1 | 5G | WAN（连接校园网） | 速度快，干扰少 |

### 5.4 配置 STA 模式

备份原配置：
```bash
cp /etc/config/wireless /etc/config/wireless.bak
cp /etc/config/network /etc/config/network.bak
cp /etc/config/firewall /etc/config/firewall.bak
```

配置 radio1 为 STA 模式：
```bash
# 改为 STA 客户端模式
uci set wireless.default_radio1.mode='sta'
uci set wireless.default_radio1.ssid='henu-student'
uci set wireless.default_radio1.encryption='none'
uci delete wireless.default_radio1.key 2>/dev/null
uci set wireless.default_radio1.network='wwan'
uci set wireless.radio1.channel='auto'
uci commit wireless
```

> 如果你的校园网 WiFi 有密码（WPA-Enterprise 等），请参阅附录 B 的特殊情况处理。

### 5.5 创建 wwan 网络接口

```bash
uci set network.wwan='interface'
uci set network.wwan.proto='dhcp'
uci commit network
```

### 5.6 配置防火墙

```bash
uci add_list firewall.@zone[1].network='wwan'
uci commit firewall
```

### 5.7 应用并验证

```bash
wifi reload
sleep 5

# 检查 STA 连接状态
iw dev phy1-sta0 link

# 让它获取 IP
ifup wwan
sleep 3

# 看看拿到了什么 IP
ip addr show dev phy1-sta0 | grep inet
```

如果输出里有 `inet 10.x.x.x`，说明从校园网 DHCP 服务器拿到了 IP。

网络测试：
```bash
# 测试校园网内网（应该通）
ping -c 2 172.29.35.27    # 认证服务器

# 测试外网（此时应该不通，因为还没认证）
ping -c 2 8.8.8.8
```

> **此时的状态**：路由器已经通过 WiFi 连上了校园网，拿到了内网 IP，能访问校园内网，但上不了外网。这就像你用手机连上 henu-student 但还没打开浏览器认证一样。

---

<a name="part6"></a>
## Part 6：部署校园网认证脚本

### 6.1 理解校园网认证流程

河大校园网使用 **Portal 认证**：

```
连上 henu-student → 拿到 IP → 仅能访问校园内网
                                    ↓
                           打开浏览器任意网页
                                    ↓
                       被强制跳转到 Portal 页面
                       (172.29.35.36:6060)
                                    ↓
                         输入学号密码点登录
                                    ↓
                            背后执行三步认证
                                    ↓
                            认证通过，外网通了
```

我们的脚本就是模拟浏览器执行这三步 HTTP 请求，去掉"手动打开网页"的环节。

### 6.2 创建认证脚本

SSH 到路由器，执行：

```bash
mkdir -p /etc/campus_network
```

然后把下面的脚本**完整复制**到 `/etc/campus_network/auto_login.sh`。

> **你需要修改的地方**：第 4-7 行的 `USERNAME`、`PASSWORD`、`OPERATOR_SUFFIX`、`CAMPUS_CODE` 改成你自己的。

```bash
cat > /etc/campus_network/auto_login.sh << 'EOF'
#!/bin/sh

# ====== 改成你自己的信息 ======
USERNAME="你的学号"
PASSWORD="校园网密码"
OPERATOR_SUFFIX="@henuyd"    # 移动: @henuyd, 联通: @henult, 电信: @henudx
CAMPUS_CODE="07cdfd23373b17c6b337251c22b7ea57"
# ============================

LOG_FILE="/tmp/campus_network.log"

log() {
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] $1" >> $LOG_FILE
}

# 获取网络参数（WAN IP、时间戳、UUID）
get_auth_params() {
    WAN_IP=$(ip route get 8.8.8.8 2>/dev/null | awk '{for(i=1;i<=NF;i++) if($i=="src") print $(i+1)}')
    TIMESTAMP=$(($(date +%s) * 1000))
    UUID=$(cat /proc/sys/kernel/random/uuid 2>/dev/null)
    echo "$WAN_IP $TIMESTAMP $UUID"
}

# 第一步：验证账号密码
first_auth() {
    log "第一步认证..."
    RESPONSE1=$(wget -q -O - \
        --post-data="campusCode=${CAMPUS_CODE}&username=${USERNAME}&password=${PASSWORD}&operatorSuffix=${OPERATOR_SUFFIX}" \
        --header="Content-Type: application/x-www-form-urlencoded" \
        --header="Referer: http://172.29.35.36:6060/" \
        "http://172.29.35.27:8088/aaa-auth/api/v1/auth" 2>&1)
    
    log "第一步响应: $RESPONSE1"
    if echo "$RESPONSE1" | grep -q '"code":1'; then
        return 0
    else
        return 1
    fi
}

# 第二步：二次校验
second_auth() {
    log "第二步认证..."
    RESPONSE2=$(wget -q -O - \
        --post-data="username=${USERNAME}&password=${PASSWORD}&operatorSuffix=${OPERATOR_SUFFIX}" \
        --header="Content-Type: application/x-www-form-urlencoded" \
        --header="Referer: http://172.29.35.36:6060/" \
        "http://172.29.35.27:8882/user/check-only" 2>&1)
    
    log "第二步响应: $RESPONSE2"
    if echo "$RESPONSE2" | grep -q '"code":1'; then
        return 0
    else
        return 1
    fi
}

# 第三步：门户最终认证
portal_auth() {
    log "第三步门户认证..."
    
    PARAMS=$(get_auth_params)
    WAN_IP=$(echo "$PARAMS" | cut -d' ' -f1)
    TIMESTAMP=$(echo "$PARAMS" | cut -d' ' -f2)
    UUID=$(echo "$PARAMS" | cut -d' ' -f3)
    
    log "使用参数: IP=$WAN_IP, TS=$TIMESTAMP, UUID=$UUID"
    
    RESPONSE3=$(wget -q -O - \
        --header="Referer: http://172.29.35.36:6060/" \
        --header="Cookie: macAuth=; ABMS=362ee66b-fa1f-4ef9-a651-bfd9d61d194a" \
        -T 10 \
        "http://172.29.35.36:6060/quickauth.do?userid=${USERNAME}%40henuyd&passwd=${PASSWORD}&wlanuserip=${WAN_IP}&wlanacname=HD-SuShe-ME60&wlanacIp=172.22.254.253&timestamp=${TIMESTAMP}&uuid=${UUID}" 2>&1)
    
    log "第三步响应: $RESPONSE3"
    
    if echo "$RESPONSE3" | grep -q '"message":"认证成功"'; then
        log "✅ 第三步认证成功"
        return 0
    else
        log "❌ 第三步认证失败"
        return 1
    fi
}

# 网络检测
check_network() {
    ping -c 2 -W 3 8.8.8.8 > /dev/null 2>&1
}

# 完整认证流程
full_auth() {
    log "开始完整认证流程..."
    
    if first_auth; then
        sleep 2
        if second_auth; then
            sleep 2
            if portal_auth; then
                log "🎉 所有认证步骤成功完成"
                return 0
            else
                log "❌ 第三步认证失败"
                return 1
            fi
        else
            log "❌ 第二步认证失败"
            return 1
        fi
    else
        log "❌ 第一步认证失败"
        return 1
    fi
}

main() {
    log "=== 校园网认证启动 ==="
    
    if check_network; then
        log "网络已连通，无需认证"
        return 0
    fi
    
    log "网络未连通，开始完整认证流程..."
    if full_auth; then
        sleep 5
        if check_network; then
            log "🎉 认证成功，网络已连通！"
        else
            log "⚠️ 认证完成但网络检测失败"
        fi
    else
        log "❌ 认证流程失败"
    fi
}

main
EOF

# 给脚本执行权限
chmod +x /etc/campus_network/auto_login.sh
```

### 6.3 手动测试

```bash
# 运行脚本
/etc/campus_network/auto_login.sh

# 查看日志
cat /tmp/campus_network.log
```

如果最后一行是 `🎉 认证成功，网络已连通！`，恭喜！

验证外网：
```bash
ping 8.8.8.8
```

如果 ping 通，说明一切正常。

### 6.4 如果认证失败了

```bash
cat /tmp/campus_network.log
```

看哪一步失败：

| 错误 | 可能原因 | 解决方法 |
|------|----------|----------|
| 第一步 code≠1 | 账号密码/运营商后缀/校编码错误 | 检查 4 个参数 |
| 第一步连不上 | 校园网没连上 | 回 Part 5 检查 STA 状态 |
| 第二步 code≠1 | 账号类型不匹配 | 确认运营商后缀 |
| 第三步 message≠认证成功 | IP/时间戳/UUID/AC参数出错 | 检查 `wlanacname` 和 `wlanacIp` 是否正确 |

---

<a name="part7"></a>
## Part 7：定时重连

### 7.1 设置 cron

校园网认证有时效（可能几小时到一天）。我们需要路由器每隔几分钟检查一次，断了就自动重新认证。

```bash
# 添加定时任务：每5分钟运行一次认证脚本
echo "*/5 * * * * /etc/campus_network/auto_login.sh" >> /etc/crontabs/root

# 重启 cron 服务使配置生效
/etc/init.d/cron restart
```

> **cron 格式说明**：`分 时 日 月 星期 命令`
> `*/5 * * * *` = 每 5 分钟执行一次

脚本有 `check_network()` 函数，如果网络已经通了就跳过认证（防止重复登录），只有不通时才执行认证流程。

### 7.2 验证 cron 是否工作

等 5-6 分钟，然后：
```bash
cat /tmp/campus_network.log
```

你应该看到类似：
```
[2026-06-04 06:30:00] === 校园网认证启动 ===
[2026-06-04 06:30:01] 网络已连通，无需认证
```

### 7.3 NAT 修复（重要！）

在某些 OpenWrt 24.10 版本中，NAT 可能不生效——路由器自己能上网，但连着的设备不行。

如果发现这个问题：
```bash
# 关闭 fullcone NAT
uci set firewall.@defaults[0].fullcone='0'
uci commit firewall
fw4 restart
```

---

<a name="part8"></a>
## Part 8：日常维护

### 查看当前状态

```bash
# WiFi 连接状态
iw dev phy1-sta0 link

# 获取的 IP
ip addr show dev phy1-sta0 | grep inet

# 认证日志
cat /tmp/campus_network.log
```

### 改了校园网密码怎么办

```bash
ssh root@192.168.1.1
vi /etc/campus_network/auto_login.sh
# 找到 PASSWORD 那一行，改成新密码
# :wq 保存退出
```

### 想改 WiFi 名称/密码

```bash
# 改 SSID
uci set wireless.default_radio0.ssid='新WiFi名'
# 改密码
uci set wireless.default_radio0.key='新WiFi密码'
# 保存生效
uci commit wireless
wifi reload
```

### 路由器挂了怎么办

**软故障**（系统起不来但 U-Boot 正常）：
- 按住 Reset 键再通电，进入 U-Boot 恢复模式
- 通过 TFTP 重新刷入固件

**恢复原厂系统**：
- 使用教程文件夹中的 `恢复原厂系统/` 中的工具

---

## 🎉 完成！

到此为止，你的 AX3000T 应该：
- ✅ 运行着 ImmortalWrt 24.10
- ✅ 通过 WiFi 连接校园网
- ✅ 自动完成 Portal 认证
- ✅ 每 5 分钟检测断线自动重连
- ✅ 发射加密 WiFi 给宿舍设备用

**最终效果**：路由器只插一根电源线，放在宿舍角落，全宿舍的设备都能通过它上网，不需要每个人自己认证。

---

<a name="appendix-a"></a>
## 附录 A：背后的原理

*本部分写给想深入理解的读者。如果你只想知道"怎么做"，可以跳过。*

### A.1 刷机到底改了什么？

```
小米原厂固件                          刷之后
┌──────────────────┐              ┌──────────────────┐
│ 小米定制 Linux    │              │ ImmortalWrt      │
│ (基于 OpenWrt 旧版)│    ──→      │ (OpenWrt 24.10)  │
│                  │              │                  │
│ 限制：            │              │ 限制：            │
│ - SSH 锁死       │              │ - ✅ 完全开放     │
│ - 不能装任意软件  │              │ - ✅ 几千个软件包  │
│ - 分区碎片化      │              │ - ✅ 大分区 78MB  │
│ - 小米后台控制    │              │ - ✅ 你完全控制    │
└──────────────────┘              └──────────────────┘
```

### A.2 为什么重启不会丢配置？

路由器没有硬盘，用的是 NAND Flash（像 U 盘一样的存储芯片）。OpenWrt 用了一个叫 **overlayfs** 的技术：

```
你看到的完整文件系统 (/)
    ↑
┌──────────────────┐
│  /overlay (可写)   │  ← 你的修改存在这里（ubifs，重启保留）
├──────────────────┤
│  /rom (只读)       │  ← 原始固件（squashfs，永远不变）
└──────────────────┘
```

- 修改一个文件 → 写到 overlay 层 → 覆盖掉 rom 的同名文件
- 删掉 overlay → 恢复出厂设置
- 重启 → overlay 还在 NAND Flash 上 → 配置不丢

### A.3 NAT 是什么？为什么需要它？

```
你的手机 (192.168.1.x) → 外网
  私有IP，公网不可路由
         ↓
   路由器 NAT
    换头术：源IP 192.168.1.x → 10.36.82.65
         ↓
   外网看到流量来自 10.36.82.65（路由器的校园网IP）
         ↓
   外网回复 → 10.36.82.65 → 路由器反向 NAT → 192.168.1.x
```

没有 NAT，宿舍设备无法上网（私有 IP 在公网上是"不可见"的）。

### A.4 两个 radio 怎么分工？

```
radio0 (2.4G) = AP 模式
    发射 SSID "你的WiFi名"
    宿舍设备连接
    信道 1, 20MHz
    穿墙好、覆盖广
    
radio1 (5G) = STA 模式
    连接 henu-student
    作为上网出口
    信道自动选择
    速度快、干扰少
```

---

<a name="appendix-b"></a>
## 附录 B：常见问题 FAQ

### Q1：刷机中途断电了，路由器现在没反应了怎么办？

> 如果 U-Boot 还在（mtd0-mtd4 没损坏）：按住 Reset 键通电，进入 U-Boot 恢复模式，通过 TFTP 重新刷入固件。
>
> 如果是"硬砖"（BL2 坏了）：需要用 TTL 串口和编程器修复。**只要你不去动 mtd0-mtd4，这种情况几乎不会发生**。

### Q2：为什么我的 WiFi 信号比原厂固件差？

> 可能 Factory 分区（WiFi 校准数据）损坏了。检查 `step1解锁ssh/xmir-patcher-main/backups/` 里的备份还在不在。如果丢了，只能用别人的同型号备份（信号也会差一些）。

### Q3：认证脚本运行失败，日志显示连不上认证服务器？

> 检查 STA 连接是否正常：`iw dev phy1-sta0 link`
> 检查 IP 是否拿到：`ip addr show dev phy1-sta0`
> 如果 STA 断了，可能是校园网 AP 踢了你。`wifi reload` 重新连接。

### Q4：手机连 WiFi 显示"无互联网"但实际能上网？

> 这是手机系统的误报。手机通过访问特定 URL 判断网络状态，校园网有时会干扰这个检测。以浏览器实测为准。

### Q5：校园网是 WPA-Enterprise（需要输用户名密码的那种 WiFi），不是开放 Portal，怎么处理？

> 需要安装 `wpa-supplicant`，配置 `/etc/wpa_supplicant.conf`，使用 802.1X 认证（PEAP/TTLS 等）。具体方法查阅你学校网络中心提供的 Linux 连接指南。

### Q6：我想换回小米原厂系统怎么办？

> 使用教程文件夹中的 `恢复原厂系统/` 工具。里面有小米官方固件和修复工具。

---

<a name="appendix-c"></a>
## 附录 C：拓展玩法

路由器不只是"上网工具"。刷了 OpenWrt 后，它是一台真正的 Linux 服务器。以下是一些你可以探索的方向：

### 🌱 入门

| 玩法 | 命令 | 效果 |
|------|------|------|
| 广告拦截 | `opkg install luci-app-adblock` | 全网设备去广告 |
| 流量监控 | `opkg install luci-app-nlbwmon` | 看谁在占用带宽 |
| 访客网络 | 新增一个隔离的 WiFi | 客人上网但看不到你的设备 |

### 🌿 进阶

| 玩法 | 命令 | 效果 |
|------|------|------|
| 文件共享 | `opkg install samba4-server` | 插 U 盘变 NAS |
| VPN 服务器 | `opkg install luci-app-wireguard` | 从任何地方访问宿舍网络 |
| DDNS | `opkg install luci-app-ddns` | 用域名访问路由器 |
| DNS 加密 | `opkg install https-dns-proxy` | 防 DNS 劫持 |

### 🏗️ 高级

| 玩法 | 说明 |
|------|------|
| Docker | 跑容器服务（256MB RAM 偏小，能跑轻量的） |
| 透明代理 | 国内外分流（需遵守法律法规） |
| IPv6 公网 | 每个设备都能有公网 IPv6 地址 |

---

## 教程修订历史

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-06-04 | v1.0 | 初版，覆盖从刷机到自动认证的完整流程 |

---

> 本教程基于真实项目——河南大学宿舍 AX3000T + henu-student 校园网自动认证。
> 如有问题或改进建议，欢迎交流。
